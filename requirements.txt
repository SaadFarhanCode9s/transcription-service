# ============================================================
# Core API Framework
# ============================================================
# FastAPI chosen for: async-native, automatic OpenAPI docs,
# Pydantic integration, and high I/O throughput.
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# ============================================================
# Settings & Configuration
# ============================================================
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ============================================================
# File Upload & MIME Detection
# ============================================================
python-multipart>=0.0.9
# python-magic wraps libmagic to detect true file types from
# magic bytes, preventing extension-spoofing attacks.
# Requires system package: libmagic1 (apt) or libmagic (brew)
python-magic>=0.4.27

# ============================================================
# Audio Processing
# ============================================================
# numpy: Required by both Whisper backends for audio array ops.
numpy>=1.26.0

# ============================================================
# Transcription Backends (install one or both)
# ============================================================
#
# OPTION 1 (preferred): WhisperX — faster inference via CTranslate2
# quantization, word-level timestamps, forced alignment.
#   pip install whisperx
#
# OPTION 2 (fallback): OpenAI Whisper — the original library,
# simpler installation, segment-level timestamps only.
#   pip install openai-whisper
#
# The service auto-detects which is installed and uses WhisperX
# if available, falling back to openai-whisper transparently.
#
# Install at least one before running the service:
openai-whisper>=20231117

# ============================================================
# Testing
# ============================================================
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0        # Required by TestClient for async transport
